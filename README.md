# LLM-WIKI

Runnable seed repository for an agent-maintained Markdown wiki.

This first migration intentionally keeps README minimal. The verified system
surface lives in `AGENTS.md`, `.codex/skills/`, `wiki/config/`, `wiki/`, and
`scripts/`.
